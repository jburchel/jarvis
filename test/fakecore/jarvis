#!/bin/bash
# Fake jarvis CLI: record verbs that matter; answer the queries the adapter makes.
. "$(dirname "$0")/jarvis-env"
case "$1" in
  listen) [ "$2" = status ] && echo "${FAKE_WAKE:-inactive}" ;;
  *) printf 'jarvis %s\n' "$*" >> "$JARVIS_CACHE/spoken" ;;
esac
